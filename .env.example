# =============================================================================
# fanfiction.fyi — Environment Variables
# =============================================================================
# For local dev with Wrangler, secrets are set via:
#   wrangler pages secret put SECRET_NAME
# D1 and R2 bindings are configured in wrangler.toml, not here.
# =============================================================================